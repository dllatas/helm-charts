apiVersion: gateway.networking.k8s.io/v1

global: {}

gateways:
  - name: example-gateway
    namespace: default
    labels: {}
    annotations: {}
    spec:
      gatewayClassName: istio
      listeners:
        - name: http
          protocol: HTTP
          port: 80
