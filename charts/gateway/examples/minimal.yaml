apiVersion: gateway.networking.k8s.io/v1
gateways:
  - name: example-gateway
    namespace: default
    spec:
      gatewayClassName: istio
      listeners:
        - name: http
          protocol: HTTP
          port: 80
