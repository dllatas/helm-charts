{{- range $resource := .Values.roleBindings }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ $resource.name }}
  namespace: {{ $resource.namespace }}
  labels:
    helm.sh/chart: {{ printf "%s-%s" $.Chart.Name $.Chart.Version | trunc 63 | trimSuffix "-" | quote }}
    app.kubernetes.io/name: {{ $.Chart.Name | quote }}
    app.kubernetes.io/instance: {{ $.Release.Name | quote }}
    app.kubernetes.io/managed-by: {{ $.Release.Service | quote }}
{{- with $resource.labels }}
{{ toYaml . | indent 4 }}
{{- end }}
{{- with $resource.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
subjects:
{{ toYaml $resource.subjects | indent 2 }}
roleRef:
{{ toYaml $resource.roleRef | indent 2 }}
{{- end }}
