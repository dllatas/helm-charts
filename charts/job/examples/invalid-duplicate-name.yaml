apiVersion: batch/v1
jobs:
  - name: duplicate
    namespace: default
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: example
              image: busybox:1.36.1
              command: ["sh", "-c", "echo hello"]
  - name: duplicate
    namespace: default
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: example
              image: busybox:1.36.1
              command: ["sh", "-c", "echo hello"]
