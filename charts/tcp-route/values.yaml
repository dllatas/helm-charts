apiVersion: gateway.networking.k8s.io/v1alpha2

global: {}

tcpRoutes:
  - name: example-tcp-route
    namespace: default
    labels: {}
    annotations: {}
    spec:
      parentRefs:
        - namespace: staging
          name: tunnel
      rules:
        - backendRefs:
            - name: tunnel
              port: 7000
