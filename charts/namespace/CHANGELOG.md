# Changelog

## 0.0.2

- Added `global` schema support so the chart can be consumed cleanly as a dependency.

## 0.0.1

- Initial public release of the `namespace` chart.
- Added strict schema validation for namespace metadata.
- Added template-time guard against protected built-in namespaces.
